# チーム力6項目レーダーチャート UI設計書

## 1. 目的と設計方針

本機能は、チームの現在の戦力を「能力の高さ」だけでなく、現在選択しているフォーメーション、選手間連携、疲労、コンディションまで含めて一目で把握できるようにするものである。既存の「タッチライン戦術室」が持つ戦術ボード風の密度と、深いグリーン・ライム・アイスブルーを基調とした視覚言語を維持する。

グラフは単独の装飾ではなく、**次の試合で何が強く、どこを補強・改善すべきかを判断するための戦術パネル**として扱う。スマートフォンではグラフを最初に見せ、詳細は縦方向に展開する。デスクトップではグラフと診断カードを横並びにして、1画面内で判断できる構成とする。

## 2. 採用する6項目

| 軸 | 表示名 | 主な算出要素 | ユーザーへの説明 |
|---|---|---|---|
| 1 | 攻撃 | OF、シュート、ドリブル、CF・WG・OHの役割適合 | ゴールへ向かう力 |
| 2 | 守備 | DF、タックル、ブロック、パスカット、GK | 相手の攻撃を止める力 |
| 3 | 中盤 | パス、CH・DH・OHの能力、ポゼッション適性 | 試合の流れを握る力 |
| 4 | 連携 | 選手間リンク、役割適合、スキル連動、サイド連動 | 11人が噛み合う力 |
| 5 | 戦術適応 | システム理解力、現在フォーメーション習熟度、発揮率 | 今の布陣で実力を出す力 |
| 6 | 安定性 | 疲労、コンディション、負傷者、控えの厚み | 試合中も力を維持する力 |

各スコアは0〜100に正規化する。レーダーの面積を単純な総合点として扱わず、軸ごとの弱点を見つけることを主目的とする。総合値はレーダーの中央または右上に補助表示し、既存のMATCH READINESSのTEAM RATINGと混同しないよう「TEAM PROFILE」と表示する。

## 3. 配置場所の推奨順位

| 優先順位 | 画面 | 配置理由 | 表示内容 |
|---:|---|---|---|
| 1 | スタメン画面 | フォーメーション変更と同時に数値が変わるため、最も戦術判断に直結する | 大型レーダー、現在システム、6項目の数値、弱点診断 |
| 2 | チーム画面 | 選手の入れ替え・疲労・契約整理の結果を確認する場所 | 中型レーダー、平均値、前回比、改善候補 |
| 3 | ホーム画面 | 次節へ進む前の短い確認に適している | 小型レーダーまたは6項目の横バー、詳細画面への導線 |

最初の実装では、**スタメン画面にフル機能版、チーム画面に簡易版**を配置する。ホーム画面は情報量が多いため、レーダー本体を常時表示するのではなく「チーム力を見る」ボタンから展開する設計が安全である。

## 4. モバイルワイヤーフレーム（375px想定）

```text
┌─────────────────────────┐
│ TOUCHLINE  オービット東京 │  固定ヘッダー
│ 第1節  6位  ¥3,200,000  ✦315 │
├─────────────────────────┤
│ スタメンを組む              │
│ 4-3-3  攻撃的バランス       │
├─────────────────────────┤
│ TEAM PROFILE               │
│ 現在システム 4-3-3          │
│ ┌─────────────────────┐   │
│ │       戦術適応 83     │   │
│ │      ╱──────╲        │   │
│ │ 攻撃╱  ◆     ╲守備   │   │
│ │    ╲  レーダー ╱       │   │
│ │ 中盤╲──────╱連携       │   │
│ │ 安定性       連携       │   │
│ └─────────────────────┘   │
│ TEAM PROFILE  76           │
│ 攻撃 72  守備 68  中盤 75  │
│ 連携 79  戦術適応 83       │
│ 安定性 66                  │
├─────────────────────────┤
│ CURRENT READ               │
│ 戦術適応が強み              │
│ 4-3-3の習熟と選手理解が高く、│
│ 現在の布陣で能力を発揮。     │
│                             │
│ 改善候補                    │
│ 安定性 66  疲労の高い選手 3名 │
│ [練習画面で整える]          │
├─────────────────────────┤
│ MATCH READINESS             │
│ TEAM RATING 68             │
│ 攻撃 68  守備 67  発揮率83% │
├─────────────────────────┤
│ 戦術ボード                  │
│ ...                         │
└─────────────────────────┘
```

モバイルでは、レーダーチャートの横幅を親カードの100%とし、最大高さを300px程度に制限する。項目名をグラフの周囲に詰め込みすぎないため、グラフの下に6項目の数値リストを置く。各項目は2列グリッドで表示し、タップすると説明行が開くアコーディオン形式にする。

## 5. デスクトップワイヤーフレーム（1280px想定）

```text
┌────────────┬───────────────────────────────────────────────┐
│ CLUBHOUSE  │ DASHBOARD / スタメンを組む                     │
│            ├───────────────────────────────────────────────┤
│ ホーム     │ 4-4-2  4-3-3  4-5-1  3-4-3  3-5-2  3-6-1 ... │
│ スタメン   ├───────────────────────────┬───────────────────┤
│ チーム     │ TEAM PROFILE              │ MATCH READINESS   │
│ 成績       │ 現在システム 4-3-3        │ TEAM RATING 68    │
│ リーグ     │                           │ 攻撃 68           │
│ ...        │       6軸レーダー          │ 守備 67           │
│            │       TEAM PROFILE 76      │ 発揮率 83%        │
│            │                           │ 連携 79%          │
│            │ [攻撃][守備][中盤]         │ 最終補正          │
│            │ [連携][適応][安定]         │ 攻 +26 / 守 +19   │
│            ├───────────────────────────┴───────────────────┤
│            │ CURRENT READ                                   │
│            │ 強み：戦術適応 83 / 4-3-3の習熟が高い            │
│            │ 課題：安定性 66 / 疲労と控えの厚みに改善余地       │
│            ├───────────────────────────────────────────────┤
│            │ ROLE SYNERGY / SKILL LINK-UP / TACTICAL BOARD   │
└────────────┴───────────────────────────────────────────────┘
```

デスクトップでは、レーダーを左側の主カードに置き、右側に既存のMATCH READINESSを残す。これにより「チームの構造的な強み」と「今回の試合における即時戦力」を分離して表示できる。レーダーが高くても疲労でMATCH READINESSが低い場合や、総合能力は高くても戦術適応が低い場合を、ユーザーが誤解しにくくなる。

## 6. カードの視覚仕様

| 要素 | 仕様 |
|---|---|
| カード背景 | 既存の`--green`から`--green-2`への斜めグラデーション。薄いピッチラインの反復背景を使用 |
| 上罫線 | `2px solid var(--lime)`。TEAM PROFILEの主カードであることを示す |
| レーダー面 | ライムの半透明塗り、境界線はライム。比較表示時のみアイスブルーを使用 |
| グリッド | 20・40・60・80の4段階。線は`rgba(198,245,202,.15)`程度 |
| 軸ラベル | 8〜10pxの太字。モバイルではグラフ外ラベルを減らし、下部リストへ移す |
| 強み | ライムの数値、`80以上`を「強み」として表示 |
| 注意 | ゴールドの数値、`60未満`を「改善候補」として表示 |
| 危険 | 赤は負傷や極端な疲労など、試合継続上の警告に限定する |
| アニメーション | フォーメーション変更時にレーダー面を180〜240msで補間。`prefers-reduced-motion`では即時更新 |

## 7. 操作仕様

フォーメーションを変更した場合、レーダーの「中盤」「連携」「戦術適応」を再計算する。メンタリティやプレースタイルの変更では、「攻撃」「守備」「中盤」に小さな補正を加え、連携や戦術適応の変化は役割・習熟度が変わる場合に限定する。

レーダーの軸または数値をタップすると、画面下部に説明パネルを表示する。例えば「戦術適応」を選択した場合は、平均システム発揮率、現在フォーメーションの平均習熟度、最も適応している選手、改善余地のある選手を表示する。選択状態はカード内で保持し、既存の画面スクロール位置を変更しない。

「比較」ボタンは初期実装では設けず、まずは現在システムの単一レーダーに集中する。将来的に前節・次節・別フォーメーションとの比較を追加する場合は、アイスブルーの比較面を重ねる方式が適している。

## 8. 診断テキストの例

| 状態 | 表示文 |
|---|---|
| 戦術適応が高い | 「現在の4-3-3に慣れており、選手の能力を安定して引き出せます。」 |
| 連携が高い | 「近接リンクと役割適合が良好です。サイド連動も機能しています。」 |
| 安定性が低い | 「疲労またはコンディションの影響で、後半に戦力が落ちる可能性があります。」 |
| 守備が低い | 「CB・SBの守備能力、または守備的役割の適合に改善余地があります。」 |
| 中盤が低い | 「CH・DH・OHのパスと展開力が不足しています。中盤の補強を検討できます。」 |
| 攻撃が低い | 「前線の決定力またはドリブル突破が不足しています。」 |

診断は一度に多く表示せず、**強み1件と改善候補1件**に限定する。これにより、スマートフォンでもユーザーが次の行動を選びやすくなる。

## 9. 実装コンポーネント案

既存の`ui.ts`にすべてのSVG文字列を直接追加すると保守性が下がるため、次の小さな描画関数へ分離する。

```text
teamPowerRadar(values, options)
├─ radarGridPath()          20/40/60/80の六角形グリッド
├─ radarAxisLabels()        攻撃・守備・中盤・連携・適応・安定
├─ radarPolygon()           6値を角度変換して面を描画
├─ teamPowerLegend()        6項目の数値と状態ラベル
└─ teamPowerDiagnosis()     強み・改善候補・説明
```

`ClubSimulation.ts`側では、次のような読み取り専用の返却型を定義する。

```ts
export type TeamPowerRadar = {
  attack: number;
  defense: number;
  midfield: number;
  chemistry: number;
  tacticalAdaptation: number;
  stability: number;
  overall: number;
  strengthKey: keyof Omit<TeamPowerRadar, "overall">;
  weaknessKey: keyof Omit<TeamPowerRadar, "overall">;
};
```

値の計算は既存の`score()`、`tactics.chemistry`、`systemRate`、選手の疲労・コンディションを再利用し、レーダー専用の別ロジックで同じ値を二重管理しない。UIはスコアの読み取りだけを行い、計算責務を持たせない。

## 10. アクセシビリティとレスポンシブ要件

レーダー自体は視覚情報であるため、SVGに`role="img"`と`aria-label="チーム力 攻撃72、守備68、中盤75、連携79、戦術適応83、安定性66"`を付ける。さらに、グラフの下に同じ値をテキストで表示し、色だけで強弱を判断させない。

タップ対象は最小44px相当の高さを確保する。モバイルではグラフを横スクロールさせず、カード幅に収める。デスクトップではカード幅が広がってもグラフが過度に大きくならないよう、レーダーの最大幅を360px程度に制限する。

## 11. 実装順序

最初に`ClubSimulation.ts`へ6項目の読み取り専用集計を追加し、既存の攻撃・守備・連携・システム発揮率との数値整合性を確認する。次にスタメン画面へSVGレーダーと数値凡例を追加し、フォーメーション変更で面が変化することを確認する。その後、チーム画面へ簡易カードを追加し、最後にタップ詳細と診断文を加える。

検証では、4-3-3と3-6-1で中盤・戦術適応が変化すること、疲労を増やした選手で安定性が低下すること、連携が高い編成で連携力が上昇することを確認する。既存の試合戦力や保存データを変更せず、レーダーはまず可視化専用の読み取り機能として導入する。
